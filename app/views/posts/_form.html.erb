<%= form_for @post, html: {class: "form-horizontal"} do |f| %>
  <% if @post.errors.any? %>
    <% @post.errors.full_messages.each do |error| %>
      <%= js add_gritter(error, title: "Overtime App Notification", sticky: false) %>
    <% end %>
  <% end %>

  <div class="form-group">
    <%= f.label :daily_hours, class: "col-sm-2 control-label" %>
    <%= f.number_field :daily_hours, class: "col-sm-10", step: 0.5 %>
  </div>
  <div class="form-group">
    <%= f.label :date, class: "col-sm-2 control-label" %>
    <%= f.date_field :date, class: "col-sm-10" %>
  </div>
  <div class="form-group">
    <%= f.label :work_performed, class: "col-sm-2 control-label" %>
    <%= f.text_area :work_performed, class: "col-sm-10" %>
  </div>
  <%= render partial: 'status', locals: {f: f} if current_user.type == 'AdminUser' %>
  <div class="row">
    <div class="col-sm-10 col-sm-offset-2">
      <%= f.submit "Save", class: 'btn btn-primary btn-block' %>
    </div>
  </div>
<% end %> 
